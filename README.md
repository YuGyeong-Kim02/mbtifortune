## MBTI Fortune Teller

Next.js(앱 라우터)와 OpenAI API를 사용해 MBTI 유형과 현재 고민을 입력하면 맞춤형 운세, 행동 루틴, 럭키 아이템을 제안해 주는 간단한 웹앱입니다.

### 1. 사전 준비

- Node.js 18 이상
- OpenAI API Key

`.env.local` 파일을 만들어 아래 값을 채워 주세요.

```bash
cp .env.local.example .env.local
# 파일을 열어 실제 키를 입력
OPENAI_API_KEY=sk-...
```

### 2. 개발 서버 실행

```bash
npm install
npm run dev
```

`http://localhost:3000`에 접속하여 실시간으로 점괘를 확인할 수 있습니다.

### 3. 주요 스크립트

| 명령어        | 설명                           |
| ------------- | ------------------------------ |
| `npm run dev` | 개발 서버 실행                 |
| `npm run build` | 프로덕션 빌드 생성          |
| `npm run start` | 빌드 결과를 실행             |
| `npm run lint` | ESLint 검사                   |

### 4. 기능 개요

- 16가지 MBTI 유형 드롭다운 선택
- 상황/고민을 입력하거나 프리셋 문구 버튼으로 빠르게 채우기
- OpenAI `gpt-4o-mini` 모델을 통한 JSON 응답 파싱
- 에너지 레벨 뱃지, 3단계 행동 루틴, 럭키 아이템 안내
- 전체 UI는 반응형으로 구성되어 모바일에서도 사용 가능

### 5. 구조

```
src/
├─ app/
│  ├─ api/fortune/route.ts  # OpenAI 연동 API
│  ├─ page.tsx              # 클라이언트 UI
│  ├─ page.module.css       # 페이지 전용 스타일
│  └─ globals.css
└─ ...
```

필요에 따라 API 모델이나 UI 텍스트를 수정해 자신만의 포춘텔러로 확장해 보세요. 😊
